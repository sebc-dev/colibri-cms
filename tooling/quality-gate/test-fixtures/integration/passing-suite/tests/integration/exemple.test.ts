// Fixture : suite d'intégration simulée entièrement verte (FR-001).
import { describe, expect, it } from "vitest";

describe("suite d'intégration (fixture)", () => {
  it("réussit pour simuler une suite d'intégration entièrement verte", () => {
    expect(1).toBe(1);
  });
});
