Bloc de contenu de l'administration : fond `surface-raised`, filet `line`, pas d'ombre.

```jsx
<Card title="Photos de la page d'accueil" meta="12 photos · 8,4 Mo" actions={<Button size="sm" variant="secondary">Ajouter</Button>}>
  …
</Card>
```

`inset` pour un fond `surface-sunken` (journal de build, zone en lecture seule).
