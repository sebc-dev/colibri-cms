Champ texte d'une ligne, à placer dans un `Field`.

```jsx
<TextInput id="titre" value={titre} onChange={setTitre} placeholder="Titre de la page" />
<TextInput mono prefix="monsite.fr" value={slug} onChange={setSlug} />
```
