Étiquette + champ + aide (ou erreur), écart `space-2`.

```jsx
<Field label="Texte alternatif de la photo" htmlFor="alt" help="Décrivez la photo pour les personnes qui ne la voient pas.">
  <TextInput id="alt" value={alt} onChange={setAlt} />
</Field>
```
