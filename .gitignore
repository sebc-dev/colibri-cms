# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.astro/
.output/
.vercel/

# Cloudflare / Wrangler
.wrangler/
.dev.vars
.dev.vars.*
wrangler.toml.bak

# Local D1 / R2 / KV state
.mf/
*.sqlite
*.sqlite-journal

# Env & secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Test / coverage
coverage/
.nyc_output/
*.tsbuildinfo

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Test de mutation (régime nocturne — .github/workflows/nightly.yml)
.stryker-tmp/
reports/

# Réglages Claude Code personnels — `.claude/settings.json` est versionné et
# surveillé par `quality-config-guard` ; la variante `.local` ne l'est pas.
.claude/settings.local.json

# Copie locale de l'ancien socle, conservée hors du dépôt : git en garde déjà
# l'historique, et sans cette ligne un `git add -A` la réintroduirait.
archive/
