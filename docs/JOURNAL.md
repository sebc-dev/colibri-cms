# Journal — ColibriCMS

> Trace chronologique des phases jouées. Les fichiers restent la source de vérité de
> l'état courant ; ce journal enregistre les événements et les faits non dérivables
> (verdict analyze, premortem appliqué, issue d'un lot). Une ligne = un événement.
> Les lignes marquées (reconstitué) ont été datées depuis l'historique git.

## Socle

| Date | Phase | Résultat |
|---|---|---|
| 2026-07-17 | brief | 3 personas · 8 SC · 12 exclusions · (reconstitué) |
| 2026-07-17 | prd | 77 FR · 8 SC · (reconstitué) |
| 2026-07-17 | stack | Astro SSG + admin SSR Cloudflare · D1/R2/KV · 23 choix · 4 décisions → ADR · (reconstitué) |
| 2026-07-17 | adr | 0001..0008 · (reconstitué) |
| 2026-07-17 | contract | CLAUDE.md · 6 blocs de contraintes ADR · 4 règles de travail · (reconstitué) |
| 2026-07-31 | migrate | 3 anciens plugins à désinstaller · journal reconstitué · format-lint renseigné |
| 2026-08-01 | prd | réécrit après revue contradictoire · passé à `accepted` · 99 FR · (reconstitué) |
| 2026-08-01 | adr | ADR-0010 « modèle brouillon/publié à deux contenus » `accepted` · (reconstitué) |
| 2026-08-01 | adr | amendements 0003, 0004, 0005, 0007 selon ADR-0010 et la revue · (reconstitué) |
| 2026-08-01 | stack | réaligné sur ADR-0010 · passé à `accepted` · (reconstitué) |
| 2026-08-01 | contract | contraintes d'ADR-0010 et de la revue portées dans CLAUDE.md · (reconstitué) |
| 2026-08-01 | adr | suites de la revue du PRD réparties en 4 amendements (0003 c, 0004 b, 0007 d, 0010 b) · document de suivi éteint · (reconstitué) |

## Sécurité — remédiation de l'audit

| Date | Phase | Résultat |
|---|---|---|
| 2026-08-01 | audit | audit de sécurité du socle documentaire · 4 angles indépendants en contexte frais · 62 constats bruts → **54** fusionnés (4 critiques, 14 élevés, 26 moyens, 10 faibles) · verdict : corpus remarquable sur les risques nommés, muet sur l'injection |
| 2026-08-01 | plan | découpage en 11 lots, un par document cible · matrice de traçabilité des 54 constats · `docs/suites-audit-securite.md` |
| 2026-08-01 | remédiation | **L1** — PRD, section « Exigences transverses » `FR-100` → `FR-110` · mentions légales et information de confidentialité **rapatriées en V1** · ferme C-17d, avance A-01, B-08, B-09, B-12, C-11, C-12, D-01 |
| 2026-08-01 | remédiation | **L2** — `ADR-0011` « Frontières de contenu hostile » `accepted`, racine sécurité de la chaîne documentaire · allowlist fermée du texte riche, contexte de rendu déclaré, type réel par signature d'octets (SVG interdit), en-têtes et CSP · README ADR, `stack.md` et `CLAUDE.md` réalignés · ferme A-01, avance A-02, C-07, C-12, C-17a |
| 2026-08-01 | remédiation | **L3** — `ADR-0004` amendement (c), les suites d'ADR-0011 dans le cœur · `toBlocks()` en arbre de blocs typés (`set:html` interdit), requête D1 paramétrée, aperçu SSR **et** médias bruts sur un hôte distinct sous la même politique Access, `verifyAccessJwt` fail-closed, `against:'live-form-definition'` · ratures `includeDrafts` (ADR-0010) et « Email Routing » → « Email Service » · `stack.md` remonte la restriction `http(s)` du `LinkTarget` hors du commentaire de code · ferme D-02, D-04, avance A-03, B-02, B-05, B-07, B-10, C-01, C-15, C-17b |
| 2026-08-01 | remédiation | **L5** — `ADR-0007` amendement (e), le chemin de soumission de bout en bout · message **composé et non concaténé** (sujet constant, corps en texte brut, caractères de contrôle rejetés à l'entrée, aucun en-tête dérivé d'une valeur de visiteur) · corbeille **rendue comme texte**, délai **normatif à 30 jours**, purge par `DELETE` confiée au Cron · bornes de ce qui entre, de ce qui est composé (`price_delta >= 0`, plafonds anti-débordement) et de ce qui est calculé (plafond du total, dépassement = refus) · limite de débit à **deux étages** (périphérie + compteur par formulaire), `hostname` de `siteverify` contrôlé, *fail-closed* · destinataire hors du site (projection publique sans `recipient_email`) et hors du geste (relu depuis `form_defs` en `live` à chaque acheminement) · **`FR-112`** : soumission refusée si `publications.en_ligne ≠ 1` · zone vidéo (`ref` par expression rationnelle, embed construit, oEmbed en dur, vignette vérifiée avant R2) · `failure_reason` borné · renoncement de l'écart de total écrit pour mémoire · ferme D-10, avance B-03, B-04, B-08, B-09, B-11, C-05, C-06, C-08, C-09, C-11, C-14 |
| 2026-08-02 | remédiation | **L6** — `ADR-0003` amendement (d), la plateforme : ce qui est exposé, ce qui s'épuise, ce qui est épinglé · **`B-01` tranché** (arbitrage humain) : la route publique passe par un **motif de route unique** `<apex>/api/forms/*/submit` sur le domaine du site, **aucune exclusion Access nulle part** — le *Bypass* écarté parce qu'il n'applique aucun contrôle, ne journalise pas, ignore la méthode, et vit hors du dépôt où le portail ne peut pas le lire ; soumission **same-origin**, question cross-origin réglée par disparition · jeton D1 du build en **lecture seule, base unique, un par instance** (topologie de comptes laissée à L8, sa conséquence écrite) · Deploy Hook : régénération standard + garde-fou sur un `current_build_uuid` inconnu, qui lit sans redéclencher · tableau de quotas **repris** (jamais édité en place) avec sa colonne de menace et la ligne des 100 000 requêtes/jour, la parade de périphérie étant celle de L5 · **secrets de build ≠ secrets de runtime** (`wrangler secret put` ne provisionne que le runtime ; le jeton Builds est de runtime, le jeton D1 est le seul secret de build) · **exact-pin** (arbitrage humain) : le `catalog:` fait foi, `--frozen-lockfile` en CI, veille CVE au nightly · **facteur unique écrit comme risque accepté**, avec son chemin d'échappement (IdP tiers, sans changement de code) · tiers côté visiteur : Turnstile sans cookie tant que la pré-clairance est désactivée, embed vidéo en mode à confidentialité renforcée, **`FR-113`** pour la mention · ferme C-04, C-17g, **accepte** D-01, avance B-01, B-13, C-03, C-17h, D-07 |
| 2026-08-02 | remédiation | **L7** — `ADR-0006` amendement 2026-08-01 (le premier de sa série, non lettré), le dispositif de confiance lui-même · **`B-14` n'était pas un oubli mais une question non posée** : les quatre contrôles répondent tous à *le code fait-il ce qu'on a demandé ?*, aucun à *fait-il **aussi** autre chose ?* — le point aveugle a une forme, **tout ce qui ajoute un comportement sans retirer de conformité**, et c'est elle qui le rendait invisible · **arbitrage humain** : *seam clos + revue déclenchée par le portail* — aucun appel réseau hors d'un **fichier de seam déclaré** (règle qui ferme une topologie à moitié existante : les quatre chemins sortants sans nom — Deploy Hook, API Builds, API REST D1 du build, oEmbed — le prennent ici), **allowlist versionnée** d'hôtes par seam y compris « aucun hôte » pour les *bindings*, et un hôte arrivant **comme donnée** (`thumbnail_url` d'oEmbed) validé avant l'appel · **renversement partiel du brief écrit frontalement**, tenable parce que le **déclencheur** de la revue reste mécanique et bloquant et que la surface est bornée **par la forme** ; l'alternative « aucune revue » écartée pour un motif interne — `C-17e` *est* une revue humaine · dépendances : le geste d'**approbation**, la veille CVE ayant été tranchée en L6 · **mécanisme d'application protégé de lui-même** : rallonger la liste ne suffit pas, protéger les hooks par les hooks est **auto-référent** — d'où **trois maillons**, le hook → la **CI qui relit le diff** (un hook contourné n'efface pas sa trace) → la **protection de branche hors du dépôt** ; **asymétrie assumée avec L6** — là-bas le dépôt parce que le portail peut le lire, ici hors du dépôt parce que l'agent peut l'écrire · **`ADR-0009` promu `accepted` verbatim** + note de promotion : cinq contraintes sur six tenues par le code livré, la sixième à moitié — la baseline de mutants n'est pas dans `estCheminProtege()`, donc le cliquet est désarmable, écart assigné à L10 · **`SECURITY.md`** créé, `D-09` porté par ADR-0006 sans ADR dédié · **prémisse démentie** : le dépôt est **déjà public** et `main` sans protection — le déclencheur « immédiat à l'ouverture » avait déjà sonné, la règle est écrite inconditionnelle et `D-09` reste `En cours` · avance B-14, C-17e, C-17f, D-09 |
| 2026-08-01 | remédiation | **L4** — `ADR-0010` amendement (c), ce que le modèle à deux contenus n'écrivait qu'à moitié · clés naturelles à charset fermé, engendrées une fois puis immuables, unicité sur les **deux états réunis** · invariant « rien de public hors des deux contenus » étendu aux **octets** (critère de contenu, le transport restant à ADR-0004 (c)) · `FR-111` : retrait reflété dans le délai de FR-036, fraîcheur du HTML bornée, sort des dérivés écrit comme renoncement borné · jeton de verrou = compteur entier `version`, `datetime('now')` disqualifié · ferme C-13, D-03, avance B-06, B-10 (documentaire clos) |
| 2026-08-02 | adr | **ADR-0006 amdt 2026-08-02 (b)** — la protection de `main` posée sur la forge révèle une incohérence **interne** de l'amdt (a) : « revue exigée » y était placée sur la forge, où un dépôt à mainteneur unique ne peut pas la tenir sans se contourner lui-même. Contrainte raturée et scindée — forge sans acteur en contournement + portail en check requis · relecture **humaine** portée par le marqueur d'approbation du dépôt et son contrôle CI, seuil mécanique à 1 approbation dès qu'une seconde personne peut merger. Ferme `D-09` ; troisième maillon de `C-17f` posé |
| 2026-08-02 | remédiation | **L8** — `ADR-0008` amendement (b), la distribution, les secrets, l'exploitation : dix points sur tout ce qui vit **entre le dépôt et une instance en production**, là où l'amendement (a) n'avait décrit que l'entrée et la sortie d'une instance · **trois arbitrages humains, tous retenant l'option qui *supprime* une chose plutôt que d'en encadrer une** · **`A-04`** : publication **depuis la CI par *trusted publishing* OIDC**, **aucun jeton de publication n'existe**, provenance émise, publication par jeton refusée par le paquet — même geste qu'au lot L6 sur `B-01`, la question du secret réglée par **sa disparition**, et l'interdiction « jamais depuis un poste » qui devient **mécanique** au lieu de rester déclarative ; limites écrites (le compte de registre est un point unique *déplacé* et non supprimé, d'où la 2FA qui protège désormais une configuration et non un jeton ; un *runner* auto-hébergé exigerait un nouvel ADR) · **`B-13`** : **un compte Cloudflare par client**, sur un motif qui **n'est pas celui du constat** — celui-ci argumentait par le rayon d'un jeton compromis, or le décisif est que **les quotas de l'offre gratuite se comptent par compte**, si bien qu'en compte partagé un flood chez un client consomme les 100 000 requêtes/jour et les 3 000 minutes de build **des autres**, exactement la menace de `C-04` avec une parade qui est *par instance* ; la classe entière du rejeu de jeton Turnstile disparaît en prime, le contrôle du `hostname` restant en seconde barrière · **`C-10`/`C-16`** : la « sauvegarde » du §4 **n'est pas une copie mais un point de restauration relevé** (*bookmark* Time Travel, 7 jours sur l'offre gratuite), ce qui **éteint `C-10` faute d'objet** — plus de lieu, d'accès ni de rétention à spécifier — et rend l'export de D1 hors instance **interdit** · **trouvaille** : la re-purge post-restauration réclamée par `C-10` est obtenue **par construction**, la purge étant un `DELETE … WHERE expires_at < now` du Cron **idempotent**, donc un invariant récurrent et non un événement — la forme choisie au lot L5 pour une tout autre raison l'achète gratuitement · **`C-02`** révèle ce que l'amendement (a) §c ne pouvait pas voir : le membre non nominatif résout la dépendance à **une** personne, pas le **partage d'un identifiant entre plusieurs** — d'où la rotation en **troisième geste** de la sortie, sur déclencheur et jamais sur calendrier · vérification post-migration au contenu défini et *fail-closed*, rollback **destructif** écrit comme tel, exécutant = identité d'agence · **`C-17i`** : correctif de sécurité en classe de version **poussée** (nuance du §3, jamais son renversement), **inventaire mis à jour par le déploiement lui-même**, version compromise **dépréciée et jamais dépubliée** ; raccordé aux détecteurs existants sans rouvrir les mises à jour automatiques, désactivées jusqu'à L10 · **`C-17j`** : quatre signaux d'exploitation vers l'agence par le seam `sendMail`, sans service ni hôte nouveau, un signal par changement d'état · **`D-06`** : ligne `users` conservée (autorat), adresse **neutralisée**, entrée KV supprimée, `verified_recipients` nettoyé — l'échec d'acheminement qui en découle est **voulu**, parce que visible · **`D-08`** : AES-256-GCM au repos sur D1/R2/KV, TLS de bout en bout, **juridiction `eu` posée à la création** et sans rattrapage possible, plus deux renoncements (pas de chiffrement applicatif de `payload_json`, KV non localisable) · enfin les deux volets **hors du portail** : la règle de lint **livrée activée** avec le cœur (`A-03` — **troisième terme de la série d'arbitrages de placement** : L6 dans le dépôt parce que le portail peut le lire, L7 hors du dépôt parce que l'agent peut l'écrire, L8 dans un dépôt que **ni l'un ni l'autre n'atteint**, avec l'infériorité de la barrière écrite) et mentions légales + information de confidentialité fournies au **provisionnement** (`B-12`) · ferme B-12, B-13, C-02, C-10, C-16, C-17i, C-17j, D-06, D-08, avance A-03 et A-04 — `A-04` restant **`En cours`** par divergence nommée avec `C-17g` : son mécanisme est un artefact de **ce** dépôt, vérifiable par le portail, et il n'existe pas encore (échéance Porte 2) |
| 2026-08-02 | remédiation | **L9 — dernier lot d'amendement, et clôture du chantier** · `ADR-0005` amendement 2026-08-01 (non lettré, sous-titré « les cibles de test de l'audit de sécurité ») : les huit lots précédents ont écrit des **règles**, celui-ci écrit **ce qui les prouve** — le geste qu'ADR-0010 avait fait une fois pour la fuite de brouillon, étendu à tout le reste · **dix-huit cibles groupées par frontière** (la frontière dit *où* le test s'exécute, et deux cibles d'une même frontière partagent leur montage), chacune écrite dans la forme qui la rend **déterministe** — élément non énuméré ⇒ **rejet de la valeur** et non sortie nettoyée · en-têtes vérifiés sur le HTML **réellement bâti** *et* sur la surface non fiable · routes admin derrière Access **et** soumission publique **dans le même test** · plafond du total ⇒ **refus**, jamais un montant approché · après `expires_at`, **requête D1 directe** sans quoi un filtre passerait pour une suppression · allowlist réseau des sept seams · invariants post-migration *fail-closed* · **`C-17c` tranché (arbitrage humain)** : un service token n'a pas de claim `email`, `verifyAccessJwt` **refuse** toute assertion sans `email` — donc un service token ne peut **pas** écrire, aucune seconde résolution d'identité n'entre dans le seam d'auth, et ce qui lui reste est **de prouver qu'Access est appliqué** ; alternatives nommées (identité de test dans `users` : *l'absence d'une donnée* deviendrait la seule barrière ; suppression : plus de véhicule pour la cible de `B-01`) · **`D-05`** et le résiduel de **`D-04`** raturés (« 500 builds/mois » → 3 000 minutes/mois ; « Email Routing » → Email Service) · **trois décisions de clôture écrites plutôt que contournées** : *(1)* une cible **nommée** ne fait passer personne à `Traité` — pour ces règles-là **le test *est* le mécanisme**, et l'autre lecture aurait fait verdir douze lignes au lot de clôture sans qu'un octet de vérification existe ; *(2)* **aucune porte n'est cochée** — 32 constats restent `En cours`, ce sont des travaux dus et non des risques assumés, et la **tension propre à la Porte 1** est nommée (elle se dit « avant la première ligne de code » quand douze de ses constats attendent un test qui s'écrit **avec** ce code) ; *(3)* le plan prescrivait sa propre suppression tout en portant seul **L10** et **L11** — L11 survit dans le § *Re-passe d'audit* de l'audit, **L10 migre dans `CLAUDE.md`** avec son échéance (avant le premier commit de `packages/core`) et sa contrainte d'ordre (`protected-paths.ts` se protège lui-même dès qu'on l'étend) · **deux résiduels périmés relevés** : le canal du signal de `C-03`, pris au lot L8, et la mention « `JOURNAL.md` *(recréé)* » — le fichier existait · ferme **C-17c** et **D-05**, les deux derniers `À traiter` ; `docs/suites-audit-securite.md` **supprimé** |

## 001-ci-quality-gate

| Date | Phase | Résultat |
|---|---|---|
| 2026-07-19 | kickoff-feature | dossier `001-ci-quality-gate` · greenfield · (reconstitué) |
| 2026-07-19 | specify | 30 FR · 0 [NEEDS CLARIFICATION] · (reconstitué) |
| 2026-07-19 | plan | 22 fichiers touchés · 1 candidat ADR (0009) · (reconstitué) |
| 2026-07-19 | tasks | 11 lots R1..R11 · 58 tâches · (reconstitué) |
| 2026-07-26 | run | R1..R11 livrés · 58/58 tâches cochées · portail à 11 checks, 2 hooks, CI + nightly · (reconstitué) |

## 002-mecanisation-portail

| Date | Phase | Résultat |
|---|---|---|
| 2026-08-03 | kickoff-feature | `specs/002-mecanisation-portail/` créé · cycle complet · **delta** (cible : `specs/001-ci-quality-gate/spec.md`) · lot **L10** de la remédiation d'audit |
| 2026-08-03 | specify | 28 FR ajoutés (FR-031→FR-058) · 5 modifiés (FR-013, 017, 018, 021, 023) · 8 SC · 3 [NEEDS CLARIFICATION] · périmètre : 4 résiduels d'outillage + 5 dormants + dette 001 (5 écarts code/spec) · 10 résiduels exclus avec motif |
| 2026-08-04 | clarify | **3 résolus · 0 restant** · + 8 zones sous-spécifiées relevées et tranchées (11 questions fermées) · **la preuve d'attribution de l'approbation entre dans le périmètre** — signature par clé logicielle à passphrase, vérifiée hors ligne depuis un registre versionné à auto-référence fermée, **ADR-0012 à créer en `plan`** ; l'exclusion correspondante saute et `FR-045` change de nature (fil-piège → exigence) · écart local/CI **délibéré** : couverture en local, signature exigée en intégration continue seulement · diff calculé depuis le point de divergence avec la branche par défaut · périmètre **propre à chaque** contrôle dormant, avec la limite du faux dormant écrite dans le corps · 6 FR ajoutés (FR-059→FR-064) · 5 SC ajoutés (SC-015→SC-019) · FR-036, 045, 046, 047, 058 amendés · 2 renvois erronés corrigés (FR-013→FR-046, FR-023→FR-044) |
| 2026-08-05 | plan | **34 fichiers touchés** (13 nouveaux · 21 modifiés, dont les 11 contrôles de 001) · **1 candidat ADR (`_candidates/0012-draft.md` — preuve d'attribution de l'approbation)** · `applies()` → `perimetre()` tri-état appelé par `runGate` (rend `FR-032` structurelle) · approbation liée à l'**empreinte du contenu** et signée en SSH détaché, registre lu au **point de divergence** (ferme `FR-060`) · aucune dépendance npm nouvelle · contrainte d'ordre : registre d'approbateurs **sur la branche par défaut dans un lot antérieur** à l'activation du contrôle, extension de `FR-036` en dernier · `FR-053` constatée déjà tenue (ADR-0006 amdt (b)), `FR-054` reste due · outil `bin/approuver.ts` **hors `FR`** (ergonomie) : génère et signe, **interactif et refusé hors TTY**, motif tapé au clavier — tout s'automatise **avant** la signature, rien au-delà · vérif bout-en-bout : `pnpm --filter @colibri/quality-gate test && pnpm gate` sur la branche portant sa propre approbation signée |
| 2026-08-05 | tasks | **17 lots R1..R17 · 75 tâches** · 14 lots `TDD` · 3 non-`TDD` : `R3` **inhérent** (job CI `tests-portail`), `R15` **check** (protection de branche, hors dépôt), `R17` **check** (vérif bout-en-bout) · contrainte d'ordre honorée par le découpage : amorçage du registre (`R6`) et de l'artefact + outil (`R7`) **avant** l'activation du contrôle `approbation` (`R8`, PR distincte — `FR-060` lit le registre au point de divergence), extension de `FR-036` en **`R16`, dernière écriture de l'IA sur le dépôt** · ADR-0012 accepté dans la PR de `R6` · `checks/index.ts` sérialise `R8` → `R12` → `R13` → `R14` ; seuls `R5` et `R11` sont `[P]` · les 39 `FR` rattachées chacune à **un seul** lot |
| 2026-08-05 | analyze | **CORRIGER D'ABORD** — 8 Critical · 13 Major · 21 Minor · contrat : `FR-020` de 001 (parité local/CI) cassée par `FR-061`/`SC-015` sans `[MODIFIED]` ni amendement ADR-0009, révision de lecture des deux listes de chemins non fixée (contournement par `protected-paths.ts` lui-même), `FR-048` implémente `C-17a` que le § NON inclus exclut, `FR-037` sans vérification sur 3 de ses 4 catégories, et `plan`/`tasks` font éditer par l'IA le mécanisme d'application qu'ADR-0006 lui interdit **dès `R1`** · découpage : `R4` (`scope.ts`) et l'artefact de `R7` sont des modules sans consommateur en-lot — premier verdict observable seulement en `R8` —, `R7` et `R14` portent deux sujets · seconds regards `ears-verifier` + `slice-auditor` en contexte frais |
| 2026-08-05 | specify | **passe corrective post-gate** · **43 FR · 0 marqueur** (6 `[MODIFIED]` de 001 · 37 ajoutés, `FR-031`→`FR-067`) · 16 SC (`SC-007`→`SC-022`) · **4 Critical de contrat fermés** : `FR-020` de 001 déclarée `[MODIFIED]` (parité de verdict restreinte au seul contrôle d'approbation, **aucun ADR amendé** — ADR-0009 contrainte 2 porte sur le même `runGate`, pas sur le verdict) · `FR-065` **nouvelle** — le fichier portant la source de définition unique des chemins est déclencheur **inconditionnel**, fermant l'auto-désarmement par retrait d'entrée (`SC-020`) · `FR-048` **réduite au versant `A-03`** (balisage brut), `C-17a` restant reporté · `FR-037` **réduite aux chemins possédés + registre**, les trois catégories d'ADR-0006 pt 3 sans forme de chemin partant au § NON inclus avec leur limite écrite · Major : conséquence d'une approbation **invalide** tranchée (écartée, sans annuler les autres — `SC-022`), `US5`/`FR-053` corrigées (`D-09` clos depuis le 2026-08-02, seul `FR-054` reste dû), motif d'exclusion des dix résiduels réécrit pour discriminer les cinq dormants (règle lexicale close vs objet à définir) · Minor : `FR-013` sortie de `[MODIFIED]`, `FR-014` ajoutée, `FR-060`/`FR-061` scindées (`FR-066`, `FR-067`), `FR-048`→`FR-052` en forme `If … then`, 12 renvois ADR-0009 corrigés, fuite de stack retirée, `SC-021` ajouté, `DELTA.md` remis à jour · **reste ouvert pour `plan`** : qui écrit `tooling/quality-gate/` et `.github/workflows/` pendant `R1`→`R16`, qu'ADR-0006 interdit à l'IA — pointeur posé en `H-04`, non tranché ici |
| 2026-08-05 | plan | **passe corrective post-gate** · **35 fichiers touchés** (14 nouveaux · 21 modifiés) · **2 candidats ADR** (`_candidates/0012-draft.md` — preuve d'attribution ; **`_candidates/0013-draft.md` — régime d'amorçage du mécanisme d'application**, nouveau) · **`H-04` tranché (arbitrage humain)** : ADR-0006 interdit à l'IA d'éditer le mécanisme d'application, or **31 des 35 fichiers du lot sont ces chemins** — option retenue **régime d'amorçage complet**, l'IA écrit le lot sous dérogation déclarée dont l'**expiration est mécanique** (elle s'éteint quand `FR-036` atteint la branche par défaut, le garde refusant dès lors absolument), barrière substitutive = un lot = une PR relue avant fusion ; *écartées* : lecture littérale (≈2 800 lignes à la main, et elle institue sans l'écrire une exigence qu'on n'a pas eue pour 001) et découpage par autorat (déplace le résiduel vers la logique des contrôles au lieu de le réduire) · faits matérialisés avant l'arbitrage : le garde sort `exit 0` sur les quatre chemins du mécanisme, 001 porte `Co-Authored-By: Claude` sur chaque commit de `tooling/`, `D-09` clos depuis le 2026-08-02 · **Critical de la gate fermés côté plan** : révision de lecture des deux listes fixée — prédicat évalué sur l'arbre de travail, fermeture par `FR-065` (déclencheur inconditionnel) plutôt que par un `import()` d'une autre révision, qui ouvrirait une surface d'exécution dans le module dont la valeur est d'être digne de confiance · `FR-037` réduite (les 3 catégories sans forme de chemin sortent) · `FR-048` réduite ⇒ contrôle renommé `raw-markup.ts`, `render-context.ts` promettrait `C-17a` reporté · frontière **`illisible` ⇒ `échoué` / `écartée` ⇒ remarque** écrite, `motif` sans `.min(1)` au schéma (sinon une approbation bavarde deviendrait fatale aux valides cumulées) · `FR-014`, `FR-020`, `FR-065`, `FR-066`, `FR-067` couvertes · **`tasks.md` périmé sur 5 points, à re-produire** |
| 2026-08-05 | tasks | **re-production post-gate** · **17 lots R1..R17 · 84 tâches** · 14 lots `TDD` · 3 non-`TDD` : `R3` **inhérent** (job CI `tests-portail`), `R15` **check** (protection de branche, hors dépôt), `R17` **check** (vérif bout-en-bout) · **5 points de péremption fermés** (plan § *Ce que cette passe corrective impose*) : `FR-014`/`FR-020`/`FR-065`/`FR-066`/`FR-067` désormais portées (`R1`, `R9`, `R4`), `FR-037` réduite en `R4` (ni seam, ni allowlist, ni endpoint nouveau), contrôle de `FR-048` renommé `raw-markup.ts` en `R12`, motif vide écarté **après** parsing et non par le schéma (`R8`), **ADR-0013 accepté dans la PR de `R1`** (`T1`, seule tâche hors `FR`, gouvernance) · **2 findings de découpage de la gate fermés** : `scope.ts` reçoit son consommateur en-lot (`R6` = diff **et** premier verdict `BLOQUÉ`), et les deux lots à double sujet scindés — artefact/outil en `R7`+`R8`, dormants/indépendance des périmètres en `R12`+`R13`+`R14` · forme du lot `R5` (module sans consommateur) explicitée comme conséquence de `FR-060`, pas comme couche · `checks/index.ts` sérialise `R6` → `R11` → `R12` → `R13` ; seuls `R4`, `R5` et `R10` sont `[P]` · `FR-013` retirée de la couverture (sortie de `[MODIFIED]` en `specify`), sa non-régression vérifiée dans `R10` · les 43 `FR` rattachées chacune à **un seul** lot
| 2026-08-05 | analyze | **CORRIGER D'ABORD** — 5 Critical · 16 Major · 12 Minor · couverture intacte (43/43 `FR` avec impl + vérif observable, chacune dans un seul lot ; 16/16 `SC` portés ; 0 marqueur) — les Critical sont ailleurs · contrat : la **racine d'exécution du portail n'est aucune entrée du contrat** alors que `pnpm gate` délègue par `--filter` (donc `cwd = tooling/quality-gate/`) et que `bin/gate.ts` passe `ctx: {}` — `.github/workflows/`, `pnpm-workspace.yaml`, `approbations/` et le point de divergence sont hors de cette racine, d'où un `ignoré` perpétuel que `T83` certifierait conforme · `T8` énonce « aucun des onze contrôles ne rapporte `passé` » là où `SC-010` dit « à périmètre **vérifié vide** » — tenu à la lettre, il fait basculer à `ignoré` des contrôles à périmètre non vide (`lint-format`) · `É-02` incomplet sur constat de code : `typecheck` et `versions-catalog` rendent aussi `passé` à vide, et `R10` bâtit `FR-046` par-dessus · `FR-048` ne nomme pas la forme refusée, seul dormant dont la règle n'est pas lexicalement close — sa fixture la fixerait à l'implémentation · découpage : `R5` livre `approval-signature.ts` dont le premier consommateur est `R9`, qui en révise le contrat (moitié artefact forcée par `FR-060`, moitié code à fusionner dans `R9`) · Major : ADR-0013 « avant toute autre écriture » n'est porté par aucune dépendance (`R3`/`R4`/`R5` en `dépend de : —`, deux `[P]`), `R6` arme le refus sans la levée (`R7`) — fenêtre où un déclencheur est `BLOQUÉ` sans remède —, `R7`/`R8`/`R9` à double sujet, `R14`/`R17` sans incrément propre, `R16` sans chemin de retour après `T81`, `R1` sous-budgété (migration des 11 contrôles), plan § *Ce que cette passe impose à `tasks.md`* périmé (il commande une re-production déjà faite), ADR-0013 déroge à ADR-0006 sans amendement ni lien et `CLAUDE.md` garde l'interdiction littérale, aucun geste ne clôt le § *Suivi* de l'audit ni ne corrige `docs/adr/README.md` · seconds regards `ears-verifier` + `slice-auditor` en contexte frais |
| 2026-08-05 | specify | **seconde passe corrective post-gate** · **48 FR · 0 marqueur** (6 `[MODIFIED]` de 001 · 42 ajoutés, `FR-031`→`FR-072`) · 19 SC (`SC-007`→`SC-025`) · **3 Critical de contrat fermés, 3 arbitrages** : **`É-06` nouveau** — le portail conclut sur la racine du paquet d'outillage et non sur celle du dépôt (`pnpm gate` délègue par `--filter`, `bin/gate.ts` passe `ctx: {}`, chaque contrôle retombe sur `process.cwd()`), d'où un catalogue lu à un chemin absent et un `passé` sans rien vérifier : **`FR-013` de 001 n'a jamais lu le vrai catalogue** ⇒ racine **résolue par le système, jamais fournie** (`FR-068`, `FR-069`, `SC-025`, ligne d'entrée au contrat d'E/S) · **`É-02` recompté sur constat de code** : **sept** contrôles rendent `passé` à périmètre absent et non cinq (+ `typecheck` sans `tsconfig.json`, + `versions-catalog` sans manifeste), trois rendent `ignoré`, `lint-format` a un périmètre jamais vide — sept + trois + un = onze · **`FR-048` reçoit sa forme refusée** (directive d'insertion de balisage brut, sortie du renderer portant une chaîne de balisage — ADR-0004 `## Constraints`), seul des cinq dormants dont la règle n'était pas décidable avant sa fixture ; nom exact laissé au plan comme pour les quatre autres · **l'outil de fabrication d'approbation entre au contrat** : `FR-070` refus hors terminal interactif, `FR-071` motif saisi par la personne qui approuve — deux propriétés que le plan décrivait comme la fermeture d'un vecteur sans qu'aucun `SHALL` ne les porte, alors que l'artefact est sur le chemin de `SC-007`→`SC-022` (`SC-024`) · Major : renvoi cassé du § `[REMOVED]` corrigé et **`SC-023` créé** — `FR-020` restreinte n'avait aucun critère de succès ; `DELTA.md` remis à jour (allowlist réseau retirée de ce que la feature ajoute, **ADR-0013 inscrit**, compte d'`É-02` corrigé) · Minor : `FR-061` scindée (`FR-072` porte le libellé « couvert »), `FR-046` reformulée en un seul `SHALL`, fuite de solution retirée de `SC-021`, avertissement sur les **trois** jeux de `FR-xxx` cohabitants ajouté en tête, `H-04` rappelée comme tranchée en `plan` · **non traité ici, par périmètre de phase** : les 8 findings de découpage (`tasks`) et, pour `plan`, les formes lexicales de `FR-049`→`FR-052`, la section finale périmée et le statut d'ADR-0013 vis-à-vis d'ADR-0006 et de `CLAUDE.md` |
| 2026-08-05 | plan | **seconde passe corrective post-gate** · **50 fichiers touchés** (14 nouveaux · 36 modifiés — dont les 11 contrôles **et leurs 11 fichiers de test**, et 4 documents de clôture) · **2 candidats ADR amendés, aucun nouveau** (`0012-draft` — pt 6 : les deux propriétés de l'outil de fabrication, `FR-070`/`FR-071` ; `0013-draft` — § *Relation à ADR-0006*) · **Critical `É-06` absorbé** : `repo-root.ts` **résout** la racine par remontée vers `pnpm-workspace.yaml`, **sans `git`** (ce qui sépare `FR-069` de `FR-055` — un dépôt sans historique fait échouer les contrôles qui dépendent du **diff**, pas celui du catalogue) ; `runGate` la résout **une fois** et la remet dans un `CheckContext` où elle est **obligatoire**, supprimant le repli `?? process.cwd()` de onze fichiers ; `pnpm gate` **inchangé**, la remontée rattrape la délégation par `--filter` · **deux pièges de la racine réelle nommés** : `typecheck` passerait à `ignoré` (racine réelle = `tsconfig.base.json` seul) et **perdrait la seule vérification de types du portail** — périmètre élargi à **tous les `tsconfig.json` de l'espace de travail**, ADR-0013 pt 5 interdisant de réduire une couverture existante ; `lint-format` a un périmètre **jamais vide**, redéclaré « au moins un fichier source », et reste `passé` **sans contredire `SC-010`** (qui ne parle que d'un périmètre *vérifié* vide) · `versions-catalog` lit enfin le **vrai** catalogue (`FR-013` de 001 mesurée pour la première fois) · **cinq dormants dotés de leur forme lexicale, chacune citée d'un ADR accepté** : `set:html` + `toBlocks(): string` (ADR-0004), `image/svg+xml` (ADR-0011 § 5), `z.string().url()` (ADR-0004 (c)), `${…}` dans un littéral `SELECT`/`INSERT`/`UPDATE`/`DELETE` (ADR-0004 (c)), `youtube.com/embed` et `player.vimeo.com` sans `dnt=1` (ADR-0003 (d) pt 8) · **`FR-070`/`FR-071` entrent au plan** : `bin/approuver.ts` quitte le statut « hors `FR` », son refus hors TTY est tenu par un **seam `IoApprobation`** dérivé de `process.stdin.isTTY` au point d'entrée réel — **ni `--motif` ni mode non interactif sur la ligne de commande**, un drapeau rouvrant ce que `FR-070` ferme · **statut d'ADR-0013 tranché** : il **n'amende pas** ADR-0006 (`DELTA.md` reste exact), la réconciliation vit dans `CLAUDE.md` § *Génération IA*, due dans la PR qui le promeut · **gestes documentaires de clôture nommés** (aucune `FR` ne les porte, et ils sont dus) : `CLAUDE.md`, `docs/adr/README.md` (index + graphe + prochain n° → `0014`), `docs/audit-securite-2026-08-01.md` — **seuls** `C-17e`/`C-17f`/`C-17h`/`B-14` mécanique/`D-09` passent `Traité`, les cinq dormants gardent un résiduel hors portail et voient leur `Preuve` rétrécie · **48/48 `FR` couvertes**, `FR-013` en non-régression · section finale périmée remplacée par 4 exigences de découpage (racine **avant** `perimetre()`, `typecheck`/`lint-format` en tâches nommées, forme de l'assertion `SC-010`, gestes documentaires et promotions d'ADR) |
