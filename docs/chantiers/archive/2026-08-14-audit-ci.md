# Audit ci — 1 Major

Portée : socle · audit
Ouvert le 2026-08-14 · Actualisé le 2026-08-14 · branche `work/reprise-socle-v2` · HEAD `6320f3d`

## Objectif
Rendre `docs/ci.md` conforme : zéro Critical.

## Contexte à charger
à lire  `docs/ci.md` — le document jugé
à lire  `docs/archi.md` — les invariants, les classes hors périmètre et la convention d'homonymie
à lire  `docs/adr/` — les six ADR qui adressent des clauses à `docs/ci.md` : `0006`, `0008`, `0009`, `0012`, `0015`, `0024`

## À corriger
### Lot A — éditions dans `docs/ci.md`
- **[arch-invariants · six ADR] Major** — `l. 118`, colonne « Contrôle », annonce le job dérivé des « Invariants de `docs/archi.md` **+ six ADR** » — les six de `l. 409`. Il en rend **trois** : `ADR-0015` (`run_worker_first` bornée), `ADR-0015`·`ADR-0024` (les interdits de la CSP) et `ADR-0006` (les attributs du cookie), seuls blocs d'ADR de `.github/scripts/arch-invariants.sh` (l. 196, 209, 224). Le document se contredit deux sections plus bas : `l. 398` verse `ADR-0008` au job `test`, et `l. 421-423` déclare `0009` et `0012` rendus par **aucun** contrôle → écrire « + les clauses d'ADR du registre ci-dessous », qui ne se périme pas, ou « + trois clauses d'ADR (`0006`, `0015`, `0024`) ». Le bandeau de `.github/scripts/arch-invariants.sh:5-6` porte le même écart, hors du document jugé.

## Prochaine étape
`/scd-sdd:resume audit-ci` pour traiter le lot A. Aucune relance d'audit n'est due : le lot ne
tient qu'à une ligne et ne dépend d'aucun amont.

## Issue
**Fermée le 2026-08-14, en 2 passes.** Les sept findings de la passe 1 sont tous tombés, par
édition de `docs/ci.md` entre les deux passes — le fichier est passé de 690 à 725 lignes et est
entré dans l'historique au même commit (`6320f3d`).

- **[ADR-0006 · anti-CSRF]**, le Critical — `l. 421-423` compte désormais **trois** clauses
  rendues par aucun contrôle au lieu de deux, nomme le jeton anti-CSRF doublé d'`Origin`, et
  `l. 686-693` le développe : la moitié « cookie » est une chaîne littérale greppable, l'autre
  une propriété de couverture qui attend l'épreuve `SC-021` au niveau specs.
- **[bloquants sans mesure]** — `l. 148-174` pose deux catégories d'exemption, rattache les quatre
  détecteurs tiers nommément, et écrit la réserve sur `workflow-integrity` et sa sortie.
- **[à compléter]** — quatre marqueurs ramenés à deux, `l. 70` et `l. 385`, tous deux dans le cas
  que le `<completion>` autorise (une commande explicitement à poser, jamais inventée).
- **[classe 13]** — plus aucune occurrence ; `l. 701` vise la **classe 15**, qui porte bien « la
  posture de sécurité de l'administration » (`docs/archi.md:179-181`).
- **[ADR-0013]** — `l. 142-144` dit l'inverse de ce qu'il disait : l'ADR décide de l'authenticité
  de l'oracle, jamais de la part de code traversée.
- **[I5 · C5]** — le qualifiant « socle » est posé `l. 10`, `l. 106` et `l. 454` ; `C5` résout à
  `docs/socle-de-livraison.md:231`, le socle `I5` à `:93`.
- **[references/archi.md]** — `l. 404` et `l. 443` donnent la provenance réelle du constat
  dependency-cruiser, avec sa date et la réserve `18.1.1`.

Le Major du lot A ci-dessus est **neuf**, relevé à la passe 2, et n'a **pas** été arbitré : la
correction reste due. Aucune entrée d'`## Écarté` n'a été ouverte sur ce document, ni à la passe 1
ni à la passe 2.
