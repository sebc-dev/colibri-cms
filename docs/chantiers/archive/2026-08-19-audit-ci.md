# Audit ci — 0 Critical · 2 Major

Portée : socle · audit
Ouvert le 2026-08-19 · Actualisé le 2026-08-19 · branche `main` · HEAD `d2bc478`

## Objectif
Rendre `docs/ci.md` conforme : zéro Critical. Le document décrivait l'avant-scaffold ; le dépôt
porte du code depuis le 15/08, et quatre reprises l'avaient mis à jour par endroits seulement.

## Contexte à charger
à lire  `docs/ci.md` — le document jugé
à lire  `.github/workflows/nightly.yml` — les deux jobs nocturnes et leur garde de scaffold
à lire  `docs/archi.md` — les invariants `I1`–`I10` et leurs traces
à lire  `eslint.config.boundaries.js` — ce que le job `boundaries` déclare réellement rendre

## À corriger
### Lot A — éditions dans `docs/ci.md`
- **[garde de scaffold] Major** — `l. 41-45` nomme « les **cinq** jobs qui la portent » (`lint`, `boundaries`, `build`, `test`, `coverage`) et `l. 891-893` oppose « les cinq jobs gardés » aux « deux nocturnes » ; `nightly.yml:36` et `:66` portent la même garde au caractère près → écrire **sept**, et ranger `dead-code` et `mutation` parmi les gardés. Sans quoi, le jour où le premier test fera tomber le point 3, deux gardes resteront dormantes dans les deux seuls jobs qui vivent des tests.
- **[boundaries · reliquat d'`I3`] Major** — raté des passes 1 et 2, hors du décompte qui a décidé du verdict : la ligne `boundaries` du tableau `l. 132` annonce que le job couvre « `I1` **et le reliquat d'`I3`** » ; `l. 86`, `l. 512`, `l. 841-847` et `eslint.config.boundaries.js:1-5` disent tous que la config déclare « la matrice d'`I1` seule » → retirer le reliquat d'`I3` de la colonne *Contrôle*. Le `<completion>` de la référence `ci` l'interdit nommément : « aucun mode couvert par un contrôle qui ne le détecte pas ».

### Lot C — renvois et signalements
- **[CLAUDE.md] signalement aval** — la gotcha « Le dépôt ne porte encore aucun code » et la ligne « Run local : aucune commande retenue à ce jour » recopient un état que `docs/ci.md:72` contredit désormais → `/scd-sdd:revise-contract`. La condition « après le lot A » que portait ce signalement est levée : le lot A est fait.

## Prochaine étape
`/scd-sdd:resume audit-ci` pour les deux Major du lot A, puis `/scd-sdd:revise-contract`.

## Écarté
Aucun arbitrage : les six findings du 19/08 ont été corrigés, et les deux Major restants ont été
retenus « à corriger » au gate du même jour.

## Issue
Refermé en **2 passes**, le 2026-08-19, sur verdict `CONFORME` (0 Critical).

Les **6 findings de la passe 1** ont été corrigés et vérifiés contre le dépôt, pas contre le texte :
les deux Critical — le § « L'état du dépôt » réécrit au constat daté du 15/08 (commit `27895a4`,
treize fichiers vérifiés suivis par git, sept fichiers sous `src/` recomptés) et les trois passages
qui tenaient « il n'y a pas de code » comme prémisse portante, dont le motif des informatifs a
changé de nature au lieu de disparaître ; les quatre Major — trois entrées d'`allowed_signers` avec
la seule qui signe encore, renvoi vers `ADR-0031` posé, `eslint-plugin-boundaries` aligné sur le
`7.1.0` du lockfile, plafond TypeScript fermé sur le pair `>=4.8.4 <6.1.0` de `typescript-eslint`.
Le signalement hors document est refermé de son côté : `ci.yml` liste dix bloquants, et
`arch-invariants.sh` nomme les six ADR du registre.

Ce qui reste est **deux Major non arbitrés et un signalement aval**, portés ci-dessus : ils ne
bloquent rien — le verdict ne compte que les Critical — mais ils sont du travail réel, et le second
Major est un défaut que les deux passes ont laissé passer.
