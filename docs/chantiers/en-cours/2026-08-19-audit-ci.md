# Audit ci — 2 Critical · 4 Major

Portée : socle · audit
Ouvert le 2026-08-19 · Actualisé le 2026-08-19 · branche `main` · HEAD `79c469b`

## Objectif
Rendre `docs/ci.md` conforme : zéro Critical. Le document décrit l'avant-scaffold ; le dépôt porte
du code depuis le 16/08, et quatre reprises l'ont mis à jour par endroits seulement.

## Contexte à charger
à lire  `docs/ci.md` — le document jugé
à lire  `docs/archi.md` — les invariants `I1`–`I10` et leurs traces
à lire  `docs/adr/0031-gestionnaire-de-paquets-npm.md` — l'ADR qui trace vers ce document sans réciproque
à lire  `docs/adr/_candidates/typescript-plafonne-a-la-branche-6.md` — le plafond qui protège `boundaries`
à lire  `.github/allowed_signers`, `package.json`, `.github/workflows/ci.yml` — les artefacts que le document décrit

## À corriger
### Lot A — éditions dans `docs/ci.md`
- **[état du dépôt] Critical** — `l. 25-36` affirme « Le dépôt ne porte aucun code : ni `package.json`, ni `package-lock.json`, ni source » et `l. 58-64` marque sept commandes « **Normative** — à honorer au scaffold », quand les sept existent dans `package.json` et que la garde de scaffold est dormante (`ci.yml:54`) → réécrire le § au constat du jour et passer les sept cellules `État` à « Réelle », comme `npm run dev` `l. 57` l'est déjà.
- **[trous périmés] Critical** — trois passages tiennent encore « il n'y a pas de code » comme prémisse portante : `l. 131` (seul motif gardant `sast`, `coverage`, `arch-invariants` informatifs), `l. 517-519` (« le jour où il y en aura » — le jour est arrivé), `l. 822-825` (`I1`/`I3` « non posé », quand `l. 455` déclare `I1` **Rendu** par `boundaries`, job existant `ci.yml:98`) → reprendre les trois, et trancher le statut des trois informatifs : lancer la mesure par rejeu, ou réécrire le motif de report sur autre chose que l'absence de code.
- **[allowed_signers] Major** — `l. 314-315` annonce « les **deux** entrées » ; le fichier en porte trois, dont deux à `valid-before` dépassé (`20260810`, `20260816`) → recompter, et dire laquelle signe encore.
- **[ADR-0031] Major** — `ADR-0031` trace vers « `docs/ci.md` § L'état du dépôt » ; zéro occurrence de `0031` dans les 849 lignes, et `l. 38-46` arbitre encore `npm` en prose → poser le renvoi, alléger la prose que l'ADR porte désormais.
- **[eslint-plugin-boundaries] Major** — `l. 474` et `l. 542` écrivent « 7.2.0 » en version **retenue** ; `package.json:34` déclare `^7.1.0`, résolu `7.1.0` au lockfile → aligner, ou distinguer « disponible au registre » de « retenue ».
- **[plafond TypeScript] Major** — `l. 490` raisonne encore sur « ce que le scaffold installera » ; le scaffold a installé `typescript@6.0.3` et le candidat du 15/08 plafonne la branche à 6 pour garder vivante la chaîne ESLint qui rend `I1` → fermer la question et nommer le plafond comme contrainte du job `boundaries`.

### Lot C — renvois et signalements
- **[CLAUDE.md] signalement aval** — la gotcha « Le dépôt ne porte encore aucun code » et la ligne « Run local : aucune commande retenue à ce jour » recopient l'état périmé de `docs/ci.md`, que `l. 57` contredit déjà → `/scd-sdd:revise-contract`, **après** le lot A.
- **[ci.yml · arch-invariants.sh] signalement hors document** — le commentaire d'en-tête `ci.yml:8-10` liste neuf bloquants (`specs-integrity` manquant) ; `arch-invariants.sh:6-9` nomme deux ADR non rendus quand `docs/ci.md` en compte trois → à reprendre avec le lot A, mêmes fichiers que le document décrit.

## Prochaine étape
`/scd-sdd:resume audit-ci` pour traiter le lot A, puis relancer `/scd-sdd:audit ci`.

## Écarté
Aucun arbitrage : les quatre Major ont tous été retenus « à corriger » au gate du 19/08.
