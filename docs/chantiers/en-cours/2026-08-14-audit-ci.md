# Audit ci — 1 Critical · 6 Major

Portée : socle · audit
Ouvert le 2026-08-14 · Actualisé le 2026-08-14 · branche `work/reprise-socle-v2` · HEAD `2a09bcb`

## Objectif
Rendre `docs/ci.md` conforme : zéro Critical.

## Contexte à charger
à lire  `docs/ci.md` — le document jugé
à lire  `docs/archi.md` — les invariants, les classes hors périmètre et la convention d'homonymie
à lire  `docs/adr/` — les six ADR qui adressent des clauses à `docs/ci.md` : `0006`, `0008`, `0009`, `0012`, `0015`, `0024`

## À corriger
### Lot A — éditions dans `docs/ci.md`
- **[ADR-0006 · anti-CSRF] Critical** — `docs/adr/0006-auth-implementation-maison-sur-d1.md:82-85` verse aux contrôles bloquants « les attributs du cookie **et le jeton anti-CSRF doublé d'`Origin`** » ; `l. 373` ne rend que le cookie, et `csrf` / `Origin` / `SC-021` n'ont aucune occurrence dans le document → ajouter la clause au registre `l. 353-376` si elle est greppable, sinon à « Ce que ces contrôles ne couvrent pas », et porter le décompte de `l. 400` de deux à trois clauses non rendues.
- **[bloquants sans mesure] Major** — `sca`, `dependency-review`, `secrets` et `workflow-integrity` bloquent sans mesure de faux positifs ni rattachement à l'exemption de `l. 145-150`, alors que `l. 126` énonce la règle → étendre ce paragraphe à une seconde catégorie (détecteurs dont le mode de restitution élimine le faux positif à la source : `--results=verified`, épinglage vérifié) et y rattacher les quatre nommément.
- **[à compléter] Major** — quatre marqueurs hors du cas que le gabarit autorise (`l. 70` et `l. 361` sont conformes, une commande ou un rendu qui n'existe pas) → `l. 420` « au scaffold » ; `l. 460` et `l. 462` « à poser : … » ; `l. 651` « non posé, dependency-cruiser au scaffold ».
- **[classe 13] Major** — deux renvois désignent la mauvaise classe de `docs/archi.md`, dont `l. 171-173` ne contient que `SC-004`, `SC-005`, le bail de publication et l'expiration des sessions → `l. 658` « refusé faute de trace observable » (la liste est à `docs/archi.md:191-195`) ; `l. 668` « classe 15 » (`docs/archi.md:179-181`, la posture de sécurité de l'administration).
- **[ADR-0013] Major** — `l. 140-143` fait porter à `ADR-0013` (Tests — Vitest dans `workerd`) le souci du code non testé atteignant `main`, quand l'ADR porte l'authenticité de l'oracle, et que `l. 116` dit elle-même que `coverage` « mesure l'exécution, jamais l'assertion » → retirer le renvoi, ou le remplacer par le mode 1 de la grille des cinq modes.
- **[I5 · C5] Major** — symboles homonymes cités sans le qualifiant que `docs/archi.md:16-19` impose (« Ce document écrit toujours « socle `I3` » quand il désigne les premiers ») → `l. 430` « par le socle `I5` » ; `l. 106` « garde-fou du socle `C5` ».
- **[references/archi.md] Major** — `l. 419` source le constat dependency-cruiser 18.1.0 sur un chemin absent du dépôt (fichier interne du plugin) → donner la provenance réelle, comme `l. 380` le fait déjà pour la même version.

## Prochaine étape
`/scd-sdd:resume audit-ci` pour traiter le Lot A, puis relancer `/scd-sdd:audit ci`.
`docs/ci.md` était non suivi par git à l'ouverture de cette fiche : le commiter avant la passe 2, sans quoi l'ancre `HEAD` ci-dessus ne bornera aucun delta.
