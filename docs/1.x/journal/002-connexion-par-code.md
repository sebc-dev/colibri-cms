# Journal — 002-connexion-par-code

> Trace chronologique des phases jouées sur cette feature. Les fichiers restent la source de
> vérité de l'état courant ; ce journal enregistre les événements et les faits non dérivables
> (verdicts analyze et audit, premortem appliqué, issue d'un lot même bloqué, contrat révisé).
> Une ligne = un événement.

| Date | Phase | Résultat |
|---|---|---|
| 2026-08-19 | kickoff-feature | specs/002-connexion-par-code/ créé · cycle complet · greenfield |
| 2026-08-19 | specify | 28 FR · 8 SC · 3 [NEEDS CLARIFICATION] (amorçage de l'adresse autorisée, sort d'un code non consommé, motifs de refus distingués ou non) · FR-007 et le moyen de reprise hors périmètre |
| 2026-08-19 | clarify | 7 résolus · 0 restant — 3 marqueurs (amorçage de l'adresse autorisée, un seul code vivant, refus réglés sur le geste attendu) + 4 zones sous-spécifiées (délai plancher, forme du code, renvoi vers la connexion, forme du message) · 37 FR · 11 SC |
| 2026-08-19 | plan | 25 fichiers touchés · 1 candidat ADR (en-tetes-d-administration-poses-par-un-middleware) · `I6` et le contrôle `ADR-0006` réveillés · dérive préexistante relevée sur l'assertion de bilan de `scripts/verif-bout-en-bout.sh` |
| 2026-08-20 | tasks | 8 lots (7 TDD · 1 `check` : parcours réel joué contre `npm run dev`, et relecture du vocabulaire, hors d'atteinte d'un grep) · 43 tâches · R1 (~420 l.) et R3 (7 concepts) au plafond, assumés et motivés · 3 backrefs nommant un ADR faute de FR porteur (CSP d'administration, bilan d'`arch-invariants`) |
| 2026-08-21 | analyze | **CORRIGER D'ABORD** — 3 Critical (délai plancher de `FR-033`/`SC-003` sans cible chiffrée ; « identique octet pour octet » infaisable et périmètre de `FR-005` non tranché ; réponse « plafond » de `FR-009` sans FR porteur ni vérification) · 11 Major, aucun arbitré · 12 Minor |
| 2026-08-21 | specify | 44 FR · 15 SC · 0 [NEEDS CLARIFICATION] — passe de correction de gate : 3 Critical et 5 Major traités (7 FR créés : `FR-038` présence/longueur indépendantes de l'adresse, `FR-039` réponse « plafond » indiscernable, `FR-040` code non retrouvable, `FR-041` à `FR-044` en-têtes d'administration ; 4 SC créés : `SC-012` plancher ≥ 20 × le travail, `SC-013` plafond, `SC-014` trois formes de réponse, `SC-015` code non retrouvable) · `SC-003` chiffré (écart p95 ≤ 25 ms sur 100 tirs par adresse) · `FR-027` restreinte à l'appareil demandeur · 6 backrefs `FR-117` re-pointées · trou des 45 min d'enfermement nommé |
| 2026-08-21 | plan | 27 fichiers touchés (+2 : `package.json`, `package-lock.json`) · 0 candidat ADR nouveau — passe de correction de gate : 2 Critical de phase `plan` et 2 Major traités · délai plancher chiffré à 1 500 ms, dérivé du budget de travail et **gelé avant mesure** (`SC-012`) · cookie d'appareil posé au `GET`, d'où aucun `Set-Cookie` sur la soumission (`FR-038`, assertion 3d rendue faisable) · `send_email` perd `destination_address` (collision `I8` mesurée, arbitrage humain) · `@cloudflare/vitest-pool-workers` aligné sur la famille `0.21.x` d'`ADR-0013` |
| 2026-08-21 | tasks | 10 lots (9 TDD · 1 `check` : absence de route, relecture du vocabulaire et essai avec une personne, hors d'atteinte d'un grep) · 54 tâches — passe de correction de gate : 4 Major de phase `tasks` traités et 3 répercutés depuis `specify` · R1 scindé (les en-têtes d'administration sortent en R3, portés par `FR-041` à `FR-044`) · l'ancien lot d'émission scindé en R4 (fabriquer et ranger sans pouvoir relire) et R5 (faire partir) · l'ancien lot terminal dissous : chaque lot ajoute au parcours les étapes qui prouvent ses exigences · `FR-025` transverse, assumé par écrit (seule exigence dont impl et vérification ne tiennent pas dans un même lot) · R1 à hauteur de plafond (~400 l.) et migration à trois tables assumées
| 2026-08-22 | analyze | **CORRIGER D'ABORD** — 5 Critical (`SC-003` inexécutable sous le plafond de `FR-008` ; `SC-012` non observable par son porteur ; « route d'administration » non défini, `FR-035` et `SC-006` falsifiés par la feature ; décision 9 déplace la partition d'`I8` et supprime le garde-fou d'`ADR-0009` sans candidat ADR ; étape `o` posée dans R4 alors qu'elle consomme l'étape `f` de R5) · 13 Major, aucun arbitré · 12 Minor — passe 2, **intégrale** (mode dégradé : `specs/002` jamais commité, delta vide) · 7 findings de la passe 1 corrigés · **garde sur la divergence déclenchée** (`Critical + Major` : 14 → 18) · 4 des 5 Critical portent sur du texte écrit à la passe de correction · recommandation : scinder l'amas d'indiscernabilité vers la feature anti-abus
| 2026-08-22 | specify | 44 FR · 16 SC · 0 [NEEDS CLARIFICATION] — passe de correction de gate (passe 2) : **arbitrage de sortie rendu** — l'amas d'indiscernabilité reste dans `002`, corrigé sur place plutôt que scindé vers la feature anti-abus · 3 Critical et 4 Major de phase `specify` traités · `SC-003` conduite **hors plafond**, ce qui rend la campagne exécutable sous `FR-008` · `SC-012` reformulé sur une grandeur observable (étalement p5–p95 ≤ médiane / 20, sur les **mêmes** 200 mesures que `SC-003` — aucun instrument ni campagne de plus, et le critère meurt avec le plancher) · « route d'administration » défini en Légende — excepte l'écran de connexion, ne vise pas un chemin inconnu — et la table d'E/S passe à quatre lignes · « terme de développeur » énuméré en Légende (18 termes, plancher assumé) : `SC-007` devient mécanique et `SC-016` créé pour la relecture intégrale · `FR-030` aligné sur `FR-027` (borné à l'appareil) · `FR-026` borné au périmètre par le pattern `Where` — `FR-013` du PRD est différé, non refusé · `SC-001` borné : le message émis est remis intégralement, à la place de la boîte hors périmètre |
| 2026-08-22 | plan | 27 fichiers touchés · 0 candidat ADR nouveau (1 inchangé) — passe de correction de gate (passe 2) : 1 Critical et 2 Major de phase `plan` traités, plus les répercussions des corrections de `specify` · **arbitrage humain rendu, qui défait celui du 2026-08-21** : `send_email` **retrouve** sa `destination_address` — le motif du retrait était le rouge d'un contrôle **informatif** défaillant (`I8` en sous-chaîne, rejoué dans les deux sens le 2026-08-22), et la destination retourne dans le lieu qu'`I8` lui ouvre, si bien qu'aucun candidat ADR n'est à déposer et que le défaut du contrôle part à `/scd-sdd:audit archi` · `assets.run_worker_first: ["/admin/*"]` déclaré (décision 16, mesuré) — la liste que `ADR-0015` et `ADR-0026` supposaient déjà écrite n'existait nulle part, et sans elle `FR-041` reposait sur l'absence fortuite de fichier sous `public/admin/` · `juger()` corrigé : la ligne de code se cherche **par appareil, sans filtre d'état**, sinon un code expiré rendait `autre-appareil` là où `FR-031`/`SC-009` exigent « demandez-en un nouveau » · étape 3e récrite — campagne **hors plafond** en 25 salves, et les deux bornes jugées sur les **mêmes** 200 mesures ; décision 7 redérivée : 1 500 ms reste le plancher, mais comme **budget d'étalement** (médiane / 20 = 75 ms) et non plus comme multiple d'un travail inobservable · `SC-007` quitte la relecture pour l'étape 3q (grep des 18 termes sur les textes **rendus**) ; `SC-016` reste la relecture, sur **deux** porteurs — `I1` interdisant à `platform/` d'importer `admin/`, le « porteur unique » était faux · `FR-030` borné à l'appareil · `SC-004`, `SC-006`, `SC-008` reçoivent un porteur nommé |
| 2026-08-22 | tasks | 11 lots (10 TDD · 1 `check` : absence de route, vocabulaire sur les textes rendus, relecture intégrale, essai avec une personne) · 62 tâches — passe de correction de gate (passe 2) : 1 Critical et 7 Major de phase `tasks` traités, plus les répercussions de `specify` et `plan` · **arbitrage humain rendu : R1 scindé** — le refus (`R1`, la garde et le cookie) et l'écran d'accueil (`R2`) se relisent séparément, le lot tenant ~9 sujets pour 5 déclarés à hauteur de plafond ; coût chiffré : la route rend un document minimal en R1, remplacé en R2 · étape `o` descendue en R6, où naît l'étape `f` qu'elle lit — le script était injouable en R4 · `ouvrirSession` descendu en R7, avec son premier appelant · étape `ℓ` (origine étrangère → 403, défaut injecté sur `security.checkOrigin`) affectée à R5, sans porteur jusque-là · recâblage de `k` sur le cookie de `j` porté par R7/T40 · `SC-008` (parcours entier) porté par R11, R8 et R10 ajoutant des écrans après R7 · `FR-025` : critère d'acceptation local sur les **six** tâches productrices de texte, et le « porteur unique » corrigé en **deux** porteurs (`I1` interdit `platform → admin`) · `run_worker_first: ["/admin/*"]` porté par R4/T14 · `destination_address` rétablie en R6/T29, avec ses trois critères · étapes `e` (25 salves hors plafond, deux bornes sur les mêmes 200 mesures) et `m` (fenêtre vidée en reculant `emis_le`, les lignes devant survivre pour `o`) récrites · `SC-007` mécanisé en étape `q`, `SC-016` créé pour la relecture · migration à trois tables : le coût de reviewability écrit à la place du renvoi au plan · règle des **corroborations** posée en Légende (quatre tâches portent un backref sans revendiquer l'exigence) |
| 2026-08-22 | analyze | **CORRIGER D'ABORD** — 1 Critical (la mention imposée par `FR-030` porte le mot *session*, que la Légende interdit sur l'écran de saisie du code depuis que `FR-025` couvre cet écran : `T38` exige la mention, `T59` exige zéro occurrence) · 2 Major (l'étape `o` cherche une ligne que le ramassage des lignes mortes aura effacée ; la justification du `[P]` de R3 est démentie par les lignes `Fichiers :`) · 3 Minor — passe 3, **delta** sur l'ancre `2a10645`, le commit par lequel l'humain a figé l'état jugé à la passe 2 · les **18** findings de la passe précédente tous corrigés · garde sur la divergence **non déclenchée** (`Critical + Major` : 18 → 3) · 1 finding reporté hors décompte (`SC-009`, « cinq causes » pour six énumérées, texte inchangé depuis l'ancre) · arbitrage de sortie rendu : **une passe de plus** |
| 2026-08-22 | specify | 44 FR · 16 SC · 0 [NEEDS CLARIFICATION] — passe de correction de gate (passe 3) : l'unique Critical traité · `FR-030` **réécrit** — la mention de l'écran de saisie dit désormais « seul le dernier **permet d'entrer** » et non « ouvre une session », *session* étant le premier des dix-huit termes que la Légende interdit sur cet écran depuis que `FR-025` le couvre · **arbitrage rendu** : la mention cède, non l'interdit — aucune exception n'est écrite en Légende, `SC-007` reste une recherche brute des dix-huit termes · règle de non-récidive posée en Légende : un critère qui prescrit un texte visible en énonce le **sens**, jamais une phrase portant un terme interdit · seul `FR-030` était dans ce cas (les trois autres `shall` portant « session » décrivent un comportement, non un texte) · répercussions à rendre : `plan.md` l. 221 et `tasks.md` T38 citent encore l'ancienne phrase |
| 2026-08-22 | plan | 27 fichiers touchés · 0 candidat ADR nouveau (1 inchangé) — passe de correction de gate (passe 3) : le Major « étape o » traité, plus la descente du Critical `FR-030` que `specify` avait signalée · **le § Schéma D1 gagne une garde, et elle répare plus que l'étape** : une ligne de `code_connexion` n'est ramassable qu'une fois **sortie de l'heure glissante**, morte ou non — sans elle, `FR-027` annulant le code précédent à chaque demande, quatre des cinq lignes d'une rafale mouraient avant la cinquième, le ramassage les emportait, le compte de `FR-008` ne dépassait jamais une ou deux et **le plafond n'était jamais atteint** (`FR-008` à `FR-010`, `FR-039` inapplicables, `SC-005`/`SC-013` invérifiables, étape 3m lisant six messages pour cinq) · le ramassage reçoit son porteur nommé (`src/platform/auth/magasin.ts`, borne **lue** dans `regles.ts` et non recopiée : aucun second réglage ne peut dériver) · **étape 3o reportée sur le dernier code de (m)** plutôt que sur celui de (f) — la ligne de (f) était morte depuis (h) donc ramassée, et l'assertion serait restée verte quand bien même le code aurait été écrit en clair ; celle de (m) vient d'être écrite, n'a pas quitté la fenêtre, et a traversé **les deux** écritures de la vie d'un code (émission et consommation) · étape 3m garde son recul d'`emis_le`, dont le motif change : il met la fenêtre dans l'état que le § Schéma D1 décrit, au lieu d'un état que rien ne produirait en exploitation · `plan.md` l. 221 aligné sur `FR-030` — le texte prescrit à `FormulaireCode.astro` dit « permet d'entrer », l'étape 3q l'aurait autrement relevé sur le lot lui-même · confrontation aux invariants d'`archi.md` : aucune frontière franchie, aucune dérogation |
| 2026-08-23 | tasks | 11 lots (10 TDD · 1 `check` : absence de route, vocabulaire sur les textes rendus, relecture intégrale, essai avec une personne) · 62 tâches — passe de correction de gate (passe 3) : le Major `R3` traité, plus les répercussions du Critical `FR-030` et du Major « étape o » descendues de `specify` et de `plan` · **aucune frontière de lot ne bouge** : le découpage arbitré à la passe 2 tient, et rien n'est soumis de nouveau · `[P]` retiré de R3 — le marqueur promettait une disjonction que sa propre ligne `Fichiers :` démentait (`regles.ts` avec R8/R9/R10, `session/index.ts` avec R1/R7, `session/magasin.ts` avec R1), et le restreindre à des fichiers propres aurait scindé le module unique des règles pures, donc décidé à la place du plan : la chaîne est déclarée sérielle de bout en bout · étape **o** descendue de R6 en R10, derrière l'étape **m** dont elle lit désormais le dernier code — celui de **f** était mort depuis **h**, donc ramassé, et l'assertion serait restée verte sur un code écrit en clair ; T31 disparaît de R6, les tâches se renumérotent et R10 la reçoit en T57 · la **garde du ramassage** reçoit son porteur en R10/T53 (`src/platform/auth/magasin.ts`, borne lue dans `regles.ts` et non recopiée) avec le cas qui la motive en critère d'acceptation — sans elle le plafond de `FR-008` n'est jamais atteint — et T52 gagne la rafale de cinq demandes qui l'éprouve · T56 change de motif : le recul d'`emis_le` reproduit l'état du § Schéma D1 au lieu de préserver des lignes pour **o** · T37 aligné sur `FR-030` — la mention prescrite dit « permet d'entrer », et les **deux** bornes qui la tiennent sont écrites, l'appareil et le vocabulaire |
| 2026-08-23 | analyze | **PRÊT** — 0 Critical · 5 Major · 12 Minor — passe 4, **delta** sur l'ancre `2a10645`, la ligne `HEAD` de la fiche : elle avait été prise **avant** le commit humain de la passe 3, si bien que le delta a recouvert **deux** passes de corrections au lieu d'une — excès de couverture, assumé · les **3** findings de la passe 3 tous corrigés (mention de `FR-030`, étape `o` descendue en R10 avec la garde du ramassage, `[P]` de R3 retiré) · **garde sur la divergence déclenchée** (`Critical + Major` : 3 → 5), le verdict ne comptant que les Critical · 5 Major neufs, **aucun arbitré**, tous renvoyés à la correction : `FR-041` (la quatrième forme de réponse ajoutée à la passe 2 n'est tirée par aucune assertion), `SC-008` (porteur divergent plan ↔ tasks, et lieu excluant l'écran d'accueil), `FR-022`/`FR-023` (l'étape `j`, seule observation sur un cookie réel, ne porte pas leur backref), `FR-025`/`SC-007` (l'énumération d'écrans oublie l'annonce du plafond, que `T55` et `T59` vérifient), `FR-040`/`SC-015` (la spec promet une irréversibilité que la décision 14 du plan retire sans qu'elle porte la borne) · **6 findings hors décompte** — jugement sur du texte inchangé depuis l'ancre, donc ratés des passes antérieures —, consignés à l'`## Issue` de la fiche archivée · contrôle 16 sans objet (aucun `.feature`) |
| 2026-08-23 | premortem | 7 remédiations appliquées (`FR-045` échec interne indiscernable · `FR-046` durée d'utilisabilité annoncée · 1 item NON inclus : le chemin d'accès à l'administration · 3 notes de plan : durée du cookie d'appareil, atomicité du plafond, ordre de la migration `0002` · 5 tâches, T25 en R5, T40 en R7, T51 et T53 en R9, T57 en R10, renumérotation de T25 à T67) · **1 chantier ouvert** (`2026-08-23-cout-d-une-soumission-publique-de-connexion`) · 2 signalements hors cible vers `/scd-sdd:premortem socle` (recette de livraison : ordre de `0002` et chemin d'accès consigné ; détection d'une panne d'acheminement, confirmée depuis l'aval sur le canal de la connexion) · 1 risque rejeté (un `FR` de détection de panne ici aurait inventé le porteur que `docs/stack.md` réserve au socle)
| 2026-08-23 | analyze | **CORRIGER D'ABORD** — 4 Critical · 17 Major (9 arbitrés) · 14 Minor — passe 1 d'un **nouveau cycle**, ouvert par le premortem du même jour qui a touché les trois documents après un `PRÊT` ; **intégrale**, aucune fiche ouverte donc aucune ancre · Critical : `FR-040`/`SC-015` (le `shall` promet une irréversibilité que la décision 14 retire et que `T62` ne teste pas), `FR-045` (indéterminé quand c'est l'épreuve du plafond qui échoue), `T25`/`R5` (le lot `TDD` ne peut pas fermer au vert, son test portant sur des verdicts nés deux lots plus loin), étape `n` (attend la réponse `A` que la décision 8 rend impossible après `m`) · **les 5 Major de la fiche archivée le 23/08 et 5 des 6 findings de son `## Issue` étaient encore présents mot pour mot** : un `PRÊT` avait clos le cycle précédent sur une liste de travail jamais faite, et un cycle neuf sans ancre les recompte tous · 4 Major sortent directement du premortem (renumérotation `T39`→`T54` cassée, `expire_le` écrit avant sa borne, `FR-046` sans test ni étape nommée, mode `check` de R11 couvrant 4 vérifications sur 5) · 9 Major arbitrés : les 5 de dimensionnement (R1 non scindable, R5/R7, R11 horizontal par la lettre, compteurs sous-déclarés) et les 4 jamais comptés (`SC-011` confusables, `FR-039` le chemin vers le plafond est l'oracle, `FR-005` troisième réponse hors contrat, `SC-009` cinq causes pour six) · contrôle 15 joué contre `I1`–`I10`, aucune dérogation · contrôle 16 sans objet (aucun `.feature`)
